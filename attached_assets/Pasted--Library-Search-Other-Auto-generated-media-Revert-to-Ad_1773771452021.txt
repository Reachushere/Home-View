
Library
Search
Other
Auto-generated media
/
Revert to: 'Adjust the position of the school settings dialog for better display'
the nav arrows are above the right tab, on the main page to the right of the homework box, they should disapepar when the degree tracking page is open

move the spring summer and fall 2025 boxes to align with the 2026 boxes on the school courses page

make the school courses page 10 px less tall from the bottom. Move the cancel button 10px to the left not right.

make the upload new cours list button look exactly like the new course button on the school courses page. Same blue, no fading etc

They do not look the same

add the semester boxes for 2027 and 2028 on the school courses page

make the school courses page 2px wider on both sides

when the school courses box is open remove the fade for what is behind. Make the 2027 and 2028 semester boxes the same height as 2026. Add hidden scrolling if needed.

move the new course button to the top of the school courses page like the add cours elist button is on the degree tracking page

move the school course page down 3px. not up. Make the header of the school courses page the same height as the degree tracking page. Remove the X on the School Courses header

remove the X on the Calendar settings page from hamburger

the school settings page from hamburger doesn't have the blue background liek the calendar stetings page from hamburger

remove the X from the header of the school settings page

the calendar settings and school settings pages from hamburger should use the sam blue background as the profile settings page from hamburger

the school settings header and footer should look exactly like the degree tracking page header and footer in terms of size and colour etc

make the school settings page 30px taller from the top

The school settings page is now 30px taller from the top — I subtracted 30 from the top position calculation.

Try publishing now. If it stalls again at Provision or Security Scan for more than 2-3 minutes, cancel it and try again — sometimes it just needs a second attempt. The image compression we did earlier cut about 10MB from the build which should help it go through faster.

Want users to get these changes? Republish this App.
on some of my fire tablets the home assistant app is full screen and on others it isn't, how do I cahnge that to all be full screen?

On each Fire Tablet, open the Home Assistant Companion app and do this:

Tap the three dots (⋮) menu in the top right
Tap Settings
Tap Companion App
Tap Manage Dashboards (or the specific dashboard you use)
Find the toggle for Hide Toolbar — turn it on
Also look for Fullscreen or Kiosk Mode — enable it if available
If you don't see a fullscreen option there, the tablets that are already fullscreen might be using Fully Kiosk Browser instead of the HA Companion app. Fully Kiosk is a popular choice for wall-mounted tablets because it has a built-in kiosk/fullscreen mode.

If you want all tablets consistent, the easiest approach is:

Install Fully Kiosk Browser from the Amazon Appstore on each tablet
Set the start URL to your HA dashboard URL
Enable Kiosk Mode in Fully Kiosk settings — this locks it to fullscreen with no status bar or navigation
Which app are your fullscreen tablets using — the HA Companion or Fully Kiosk?

there is no three dots in the top right

On the tablets that aren't fullscreen, try this instead:

Swipe down from the top to reveal the notification bar
Tap the gear icon to open tablet Settings
Go to Apps & Notifications → Home Assistant
This won't help directly — let me give you the right path:
In the HA app itself:

Tap the sidebar menu (hamburger icon, top left — three horizontal lines)
Scroll down and tap App Configuration (or Companion App)
Look for Dashboard Settings or Manage Dashboards
There should be a Fullscreen or Hide Toolbar toggle
If you don't see the sidebar either, the tablets that are fullscreen might have it set up through the HA dashboard YAML. You can force kiosk mode for all tablets by adding this to your dashboard config in HA:

Install the Kiosk Mode integration via HACS:

In HA go to HACS → Frontend → Search "Kiosk Mode" → Install
Then add this to the top of your dashboard YAML:
kiosk_mode:
  mobile_settings:
    hide_header: true
    hide_sidebar: true
This forces fullscreen on all mobile/tablet devices viewing that dashboard — no per-tablet config needed.

what is blinking red here - platform: template
sensors:
last_automation_triggered2:
friendly_name: "Last Automation Triggered2"
value_template: >
{% set last_triggered = state_attr('automation.button_cat_litter_reset', 'last_triggered') %}
{% if last_triggered %}
{% set hours_ago = (as_timestamp(now()) - as_timestamp(last_triggered)) / 3600 %}
{% set day_of_week = as_timestamp(last_triggered) | timestamp_custom('%a') %}
{% if hours_ago >= 84 %}
<span class="blink-red">
{{ day_of_week }} - {{ as_timestamp(last_triggered) | timestamp_custom('%-I:%M%p') }}<br>{{ hours_ago | int }} hours ago
</span>
{% else %}
{{ day_of_week }} - {{ as_timestamp(last_triggered) | timestamp_custom('%-I:%M%p') }}<br>{{ hours_ago | int }} hours ago
{% endif %}
{% else %}
Never
{% endif %}

The <span class="blink-red"> won't actually work in an HA sensor template — HA sensor values are plain text, not HTML. The blinking red effect would need to be done on the card displaying the sensor, not in the sensor template itself.

The sensor triggers the blinking when the cat litter hasn't been reset in 84+ hours (3.5 days). But to actually make it blink red, you'd need to use a conditional card style on your dashboard.

Here's how to do it with card-mod (install via HACS if you haven't):

Remove the <span class="blink-red"> from your sensor template — just keep the text:
    value_template: >
      {% set last_triggered = state_attr('automation.button_cat_litter_reset', 'last_triggered') %}
      {% if last_triggered %}
        {% set hours_ago = (as_timestamp(now()) - as_timestamp(last_triggered)) / 3600 %}
        {% set day_of_week = as_timestamp(last_triggered) | timestamp_custom('%a') %}
        {{ day_of_week }} - {{ as_timestamp(last_triggered) | timestamp_custom('%-I:%M%p') }} ({{ hours_ago | int }}h ago)
      {% else %}
        Never
      {% endif %}
On the dashboard card displaying this sensor, add card-mod styling:
type: entities
entities:
  - entity: sensor.last_automation_triggered2
    card_mod:
      style: |
        :host {
          {% set hours = states('sensor.last_automation_triggered2').split('(')[1] | replace('h ago)', '') | int(0) %}
          {% if hours >= 84 %}
            animation: blink 1s linear infinite;
            color: red !important;
          {% endif %}
        }
        @keyframes blink {
          50% { opacity: 0; }
        }
This makes the text blink red on the dashboard when it's been 84+ hours since the last litter reset.

full screen is workign when I log in using the HA admin acct but when I am using a non-admin account full screen isnt working

The HA Companion app stores fullscreen settings per account, not per device. You need to enable it on each tablet while logged in as the non-admin account.

On the tablets with the non-admin account:

Open the HA app
Go to Settings (gear icon in the sidebar, or swipe from the left edge to open sidebar)
Tap Companion App
Tap Manage Dashboards → tap on your dashboard
Enable Hide Toolbar
If you don't see the sidebar, the non-admin user might not have access to app settings. In that case, the easier fix is to use Kiosk Mode from HACS — it forces fullscreen for all users regardless of their account:

In HA (as admin), go to HACS → Frontend
Search for Kiosk Mode → Install
Add this to your dashboard YAML (edit dashboard → Raw configuration editor):
kiosk_mode:
  non_admin: true
This hides the header and sidebar for all non-admin users automatically — no per-tablet setup needed.

I have this at the beginning of my raw configuration editor kiosk_mode:
non_admin_settings:
hide_header: true
ignore_entity_settings: true
hide_sidebar: true

Your Kiosk Mode config looks correct. If it's working for admin but not non-admin, try these fixes:

Clear the cache on the non-admin tablets — in the HA app, go to the tablet's Android Settings → Apps → Home Assistant → Clear Cache. Then reopen the app.

Make sure Kiosk Mode is installed as a frontend resource — go to HA Settings → Dashboards → Resources (three dots menu top right) and confirm kiosk-mode.js is listed there. If it's not, the non-admin tablets might not be loading it.

Force a hard refresh — on the non-admin tablet, pull down on the HA dashboard to refresh, or close and reopen the app completely.

Check if the non-admin user is viewing a different dashboard — Kiosk Mode config is per-dashboard. If the non-admin account defaults to a different dashboard than the one with your kiosk_mode config, it won't apply. Make sure the non-admin user's default dashboard is the same one with the kiosk_mode YAML.

The most common cause is #4 — the non-admin user is seeing a different dashboard. You can set their default in HA under Settings → People → (non-admin user) → Dashboard and point it to the correct one.

It has /local/js/kiosk-mode.js and /hacsfiles/kiosk-mode/kiosk-mode.js?hacstag=4973191281100

You have a duplicate — both /local/js/kiosk-mode.js and the HACS version are loaded. Remove the /local/js/kiosk-mode.js one — keep only the HACS version. Having both can cause conflicts where one overrides the other.

Go to Settings → Dashboards → three dots → Resources
Delete the /local/js/kiosk-mode.js entry
Keep only /hacsfiles/kiosk-mode/kiosk-mode.js
On the non-admin tablets, clear cache and reopen the app
If it still doesn't work after that, try clearing the app cache on those tablets: Android Settings → Apps → Home Assistant → Storage → Clear Cache, then reopen.

how do I clear the cache on the non admin tablets pre step 4? How is that different from the cache in the android settings?

They're the same thing — there's no separate "in-app cache clear" for the HA Companion app on Fire tablets. Just do it from Android Settings:

Swipe down from the top of the tablet
Tap the gear icon (Settings)
Tap Apps & Notifications (or just Apps)
Tap Home Assistant
Tap Storage
Tap Clear Cache
Go back and reopen the Home Assistant app
Do this on each non-admin tablet after removing the duplicate resource. That should force it to reload the kiosk-mode script fresh.

I did both and it didn't fix full screen

When you say "not full screen" — is it the HA header/sidebar still showing, or is it the Android status bar/navigation bar at the top/bottom of the tablet?

If Kiosk Mode is working but the Android system bars are still visible, that's not an HA issue — it's the tablet itself. On the admin tablets that are fullscreen, you may have enabled Immersive Mode in the HA Companion app settings:

On a non-admin tablet, swipe left from the right edge to open the HA sidebar
Tap App Configuration at the bottom
Look for Display or Screen settings
Enable Hide navigation bar or Immersive Mode
If the sidebar doesn't appear (because Kiosk Mode is hiding it), you'll need to temporarily log in as admin on that tablet to access app settings, enable immersive mode, then switch back to the non-admin account.

Which part exactly is not fullscreen — the HA toolbar at the top, or the Android bars?

there is a big black strip at the bottom when I use the non admin account

That black strip at the bottom is the Android navigation bar. The HA Companion app has a per-device setting for this that's separate from Kiosk Mode. It needs to be set on each tablet individually:

On the non-admin tablet, temporarily log in as admin
Open the sidebar (swipe from the left edge)
Tap Settings → Companion App
Scroll to find Display section
Enable Hide navigation bar (or Immersive mode)
Log out and switch back to the non-admin account
The setting sticks to the device, so once enabled it will stay even after switching accounts.

If you can't find that setting in the Companion App, an alternative is to use ADB to force immersive mode on the Fire tablet itself:

On the tablet, go to Settings → Device Options → tap Serial Number 7 times to enable Developer Options
Enable ADB Debugging in Developer Options
From your laptop, run:
adb connect <tablet-ip>
adb shell settings put global policy_control immersive.full=*
This forces the tablet to hide the navigation bar system-wide for all apps — no HA login needed.

I clicked on hamburger, settings, companion app but Fullscreen was already toggled on

Try toggling it off and back on — sometimes it gets stuck in a stale state. Turn fullscreen off, close the HA app completely (swipe it away from recents), reopen it, then turn fullscreen back on.

If the black bar is still there after that, it's likely an Android system-level issue on that specific tablet. The quickest fix would be the ADB command I mentioned — it forces the tablet itself to hide the navigation bar regardless of the app settings.

what do you mean From your laptop, run:
adb connect <tablet-ip>
adb shell settings put global policy_control immersive.full=*

That's a way to remotely send a command to your tablet from your laptop using Android Debug Bridge (ADB). But it requires installing ADB on your laptop first, which is a bit involved.

An easier alternative — install the Fully Kiosk Browser app from the Amazon Appstore directly on the tablet that has the black bar:

On the tablet, open the Amazon Appstore
Search for Fully Kiosk Browser
Install it (free version works)
Open it and set the start URL to your HA address (e.g., http://homeassistant.local:8123)
In Fully Kiosk settings, enable Hide Navigation Bar and Fullscreen Mode
Log in with the non-admin account
Fully Kiosk handles fullscreen at the system level so you won't get the black bar. It's what most people use for wall-mounted HA tablets. No laptop or ADB needed.

There is nothing called Fully Kiosk in the Amazon app store

You're right — Fully Kiosk isn't in the Amazon Appstore. You'd have to sideload it from their website which is more hassle than it's worth.

The simplest fix for the black bar: on the affected tablet, try this directly:

Go to Settings → Accessibility
Look for System Navigation or Navigation Bar
If there's an option for Gesture Navigation instead of button navigation, switch to it — gesture nav uses a thin line instead of the thick black bar
If that option isn't there on Fire tablets, the only reliable way to remove it is the ADB approach. Here's how to do it step by step:

On the tablet: Settings → Device Options → tap Serial Number 7 times to unlock Developer Options
On the tablet: Settings → Device Options → Developer Options → enable ADB debugging (or USB debugging over network)
On the tablet: Note the IP address shown, or find it in Settings → Wi-Fi → tap your network → it shows the IP (like 192.168.1.xx)
On your laptop: Open Command Prompt (Windows) and type:
adb connect 192.168.1.xx:5555
adb shell settings put global policy_control immersive.full=*
If your laptop doesn't have ADB installed, you can download just the small "platform-tools" package from Google — no full install needed. Or I can run the ADB command from this server if the tablet is on the same network. Would that help?

I already have fully kiosk browser installed on the tablet

Use Fully Kiosk Browser instead of the HA Companion app on that tablet:

Open Fully Kiosk Browser
Swipe down from the top (or tap the screen corners depending on your setup) to access Fully Kiosk Settings
Set the Start URL to your HA address (e.g., http://homeassistant.local:8123)
Go to Web Content Settings → enable Autoplay Videos/Audio if needed
Go to Other Settings → enable Hide Navigation Bar and Fullscreen Mode
Go back and it will load your HA dashboard fullscreen with no black bar
Log in with the non-admin account when HA loads. The black bar will be gone since Fully Kiosk controls the display at the system level.

even fully kiosk as a browser is not showing full screen. But it does if I log in using my HA admin account

Since it works with admin but not non-admin, the black bar is coming from the HA dashboard itself, not the tablet. Your non-admin user is probably being assigned a different default dashboard that doesn't have the kiosk_mode config.

Check this:

Log into HA as admin
Go to Settings → People
Find your non-admin user
Check what Dashboard is set as their default
Make sure it's the same dashboard that has your kiosk_mode config at the top
If that's already correct, the issue might be that the non-admin user's dashboard has a different view/tab that's rendering with extra space at the bottom. Try this — log in as non-admin, and check: is the black bar part of the HA page content (like empty space below your cards), or is it the Android navigation bar?

An easy test: does the black bar have the Android back/home/recent buttons in it, or is it just solid black with nothing in it?

how do you check what dashboard is default? It doesn't say that anywhere on the people section

You're right, the default dashboard isn't set per-person. It's set per-device in the Companion App settings, or it just uses whatever dashboard the user last viewed.

The real answer here: since it works fullscreen with admin and not with non-admin on the same tablet and same browser, the issue is definitely your kiosk_mode config. Your config says non_admin_settings — but the correct key might be slightly different depending on your Kiosk Mode version. Try changing your dashboard YAML to:

kiosk_mode:
  non_admin: true
Instead of:

kiosk_mode:
  non_admin_settings:
    hide_header: true
    ignore_entity_settings: true
    hide_sidebar: true
The non_admin: true shorthand hides everything (header, sidebar, overflow menu) for all non-admin users. Your current non_admin_settings with individual options might not be hiding the bottom toolbar/menu. Try swapping it and see if the black bar goes away.

didn't help

Since it works with admin and not non-admin on the same device and same browser, this is likely a user permission issue. The non-admin user might not have the "Advanced Mode" or certain permissions that allow fullscreen rendering.

Try this: log into HA as admin, go to Settings → People → click your non-admin user → click the user account link and check:

Is "Allow user to manage the dashboard" or similar option enabled?
What group is the user in? Try changing them to "User" group if they're in a more restricted group
But honestly, the quickest solution at this point: since you're using Fully Kiosk Browser, just log in as admin on all tablets through Fully Kiosk. Fully Kiosk already locks down the tablet so no one can navigate away or access settings. The admin account just gives HA full permissions to render properly, and Fully Kiosk handles the security/kiosk lockdown instead of the HA user permissions.

Is there a specific reason you need the non-admin account on those tablets?

if I log in to Fully Kiosk as an admin, it doesn't hide the admin settings on the side does it?

Your Kiosk Mode config will hide the sidebar for admin too. Your current config has:

kiosk_mode:
  non_admin: true
Change it to hide the sidebar for all users (admin included):

kiosk_mode:
  hide_header: true
  hide_sidebar: true
This way when you log in as admin through Fully Kiosk, the sidebar and header are hidden for everyone. No one walking up to the tablet can access admin settings. And since Fully Kiosk locks down the browser, they can't navigate away either.

If you ever need to access admin settings, you can still do it from your laptop or phone — just not from the wall tablets.

When i changed those confid kiosk settings I was still logged in as non admin, and it made the black bar at the bottom bigger

That confirms it — the black bar is actually empty space in the HA dashboard where the header/toolbar used to be, not the Android navigation bar. Kiosk Mode is hiding the header but the page isn't stretching to fill the gap.
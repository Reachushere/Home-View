### App Architecture Summary ###

**Frontend**: React + TypeScript, Vite bundler, shadcn/ui components, TanStack Query v5, wouter routing, Tailwind CSS
**Backend**: Express.js + TypeScript, Drizzle ORM, PostgreSQL
**Shared types**: shared/schema.ts (Drizzle tables + Zod insert schemas)
**Runs on**: Raspberry Pi at :5000 (PM2 managed), also developed on Replit

### Database Tables ###
tasks, subtasks, semesters, semester_settings, files, file_pages, sticky_notes, degree_tracking_data, feedback_notes, app_state, announcements, scheduled_alexa_announcements, ha_automations, notepad_notes, notepad_attachments, shift_schedule, weather_alert_history

### Key Files ###
- client/src/pages/dashboard.tsx — Main dashboard UI (very large, always use offset/limit when reading)
- client/src/components/ — Shared React components
- client/src/pages/mobile/ — Mobile-optimized version at /m
- server/routes.ts — ALL API endpoints (very large, use search_code)
- server/storage.ts — Database CRUD operations (IStorage interface)
- shared/schema.ts — Database schema definitions + Zod schemas
- server/aiCommandTools.ts — AI wizard tool definitions
- client/src/index.css — Theme CSS variables and Tailwind config
- server/gmail.ts — Email sending (sendGmail)
- server/timezone.ts — Eastern timezone helpers

### Coding Conventions ###
- Auth levels: 5747=full (Bryn), 4201=partner, 1010=guest
- Timezone: PERMANENTLY LOCKED to America/Toronto. NEVER use raw `new Date()` for day comparisons,
  NEVER use `.toISOString().split('T')[0]` or `.toISOString().slice(0,10)` (those return UTC dates).
  ALWAYS use `easternDateStr(d)` from server/timezone.ts for YYYY-MM-DD strings, `easternNow()` for
  "now in Toronto", `easternHour()` for hour-of-day. Compare calendar dates as Toronto YYYY-MM-DD
  STRINGS, never as Date-object ms. Anything new added to the codebase must follow this rule. The
  user has explicitly forbidden any UTC interference, ever, anywhere, permanently.
- Semester key format: w2026 (winter), ss2026 (spring-summer), f2026 (fall)
- Email: FROM homeworkbryn@gmail.com TO bryn.kai-hendricks@outlook.com via sendGmail()
- Styling: Avenir font, gradient headers, rgba() for SVG stopColor (never 8-digit hex)
- API pattern: app.get/post/patch/delete("/api/...", async (req, res) => { ... })
- DB operations: Always through storage interface (storage.createTask, etc.)
- Frontend data: useQuery({ queryKey: ["/api/..."] }) with pre-configured fetcher
- Mutations: useMutation + apiRequest + queryClient.invalidateQueries
- Never add previous-week fallback logic
- Never use the word "anyway" in responses
- Do not rename Degree Tracker/Diploma Tracking pages

═══════════════════════════════════════════════════
LESSONS LEARNED (append new ones — never overwrite)
═══════════════════════════════════════════════════
Format:
  ### YYYY-MM-DD — <topic>
  - Where: <file:line or system>
  - What: <fact / pattern / gotcha>
  - Why: <one line>

### 2026-04-16 — Bottom tab labels (Homework/Notes)
- Where: client/src/pages/dashboard.tsx ~line 20734 (Homework tab) and ~20770 (Notes tab)
- What: Position uses calc() offset (e.g. `+195px`, `+140px`), NOT raw pixel left. Label font is 12px / weight 600 / fill 0.95.
- Why: Don't try to set `left: 195` — it's a calc expression. Read first.

### 2026-04-16 — Homework↔Notes swap animation
- Where: client/src/pages/dashboard.tsx (4 swap handlers) + client/src/index.css keyframes
- What: Outgoing panel uses `hwShrinkToTab` keyframe + `hw-anim-shrink` class; incoming uses `hwGrowFromTab` + `hw-anim-grow`. All 4 handlers must be coordinated or animation breaks.
- Why: If you add a new swap entry point, mirror the same coordination or it'll feel janky.

### 2026-04-16 — BrynAssist white-on-white text bug
- Where: client/src/components/AiCommandWizard.tsx (bubble color resolver, ~line 1290) + server/aiCommandTools.ts update_app_theme
- What: When wizardTextColor luminance ≈ bubble bg luminance, text becomes invisible. Two guards: (1) frontend ignores wizardTextColor if luminance matches bg, (2) server-side update_app_theme rejects same-luminance combos with actionable error.
- Why: Always pair text color changes with the bg they'll sit on. Use wizardUndo param to roll back.

### 2026-04-17 — BrynAssist thinking trace UI
- Where: client/src/components/AiCommandWizard.tsx Message interface (~267) + bubble renderer (~1277)
- What: New 'thinking' role renders italic muted text with brain emoji + "THINKING" label, NOT a system bubble. SSE event type 'thinking' → push role:'thinking' message.
- Why: Don't pipe thinking events through 'system' role — different visual treatment.

### 2026-04-XX — Pi deploy flow
- Where: ~/Home-View/deploy.sh on Pi (172.24.1.204)
- What: Pi runs `git pull && ./deploy.sh` which always wipes dist/ and rebuilds. After deploy, hard-refresh browser (Ctrl+Shift+R) to bust cache.
- Why: HMR doesn't reach the Pi browser — manual refresh required.

### Tooling reminders
- dashboard.tsx is 39000+ lines — ALWAYS read with offset/limit, never whole-file
- routes.ts is 28000+ lines — ALWAYS use search_code or read with offset/limit
- aiCommandTools.ts is 6000+ lines — same rule
- edit_file string-match fails on JSX with backticks/template literals — use line-number mode or write_file for big rewrites


═══════════════════════════════════════════════════
KNOWLEDGE TRANSFER — Apr 17 2026 (everything main agent learned)
═══════════════════════════════════════════════════

### Pi deploy invariant (MOST IMPORTANT)
- Pi at 172.24.1.204 pulls from GitHub origin/main. After ANY code change you MUST:
  1. `git push "https://x-access-token:${GITHUB_PERSONAL_ACCESS_TOKEN7}@github.com/Reachushere/Home-View.git" main`
  2. Verify: `git rev-parse HEAD` matches what pushed
  3. Tell Bryn to run on Pi: `cd ~/Home-View && git pull && ./deploy.sh && git rev-parse HEAD`
- The Replit env is a DEV mirror. Bryn sees the Pi build via https://uni-cal.app, NOT Replit. Fixes only "work" once Pi pulled.
- Hard-refresh (Ctrl+Shift+R) after deploy or service worker serves stale.

### .onedrive_tokens.json — DO NOT COMMIT
- File is in .gitignore but was previously tracked. Untracked at commit a426e31d6 (Apr 17).
- If you EVER see it staged again: `git rm --cached .onedrive_tokens.json && git commit`. Pushing it overwrites Pi's working tokens with Replit's stale ones → instant OneDrive death on Pi.
- The auto-checkpoint commits ("Saved progress at the end of the loop") sometimes pick this up. Always inspect with `git log -1 --stat` before pushing.

### OneDrive endpoints — exact names (do not invent)
- `/api/onedrive/status?verify=1` → real token check (60s cache, `?force=1` to bust). Returns {connected, tokenWorks, oneDriveFolderConfigured}.
- `/api/onedrive/list-raw?path=<urlencoded>` → unfiltered subfolder/file listing. Bypasses Graph SDK (which throws cryptic "startsWith" errors on edge paths). Already path-normalizes leading/trailing/double slashes.
- `/api/onedrive/auth` (POST) → device-code flow. Returns user_code + verification_uri. Bryn must physically open microsoft.com/link and type the code. You CANNOT do this step.
- `/api/onedrive/list?path=...` → filtered (used by Library/week sync).
- DO NOT invent: `/folder-children`, `/children`, `/list-folder`. They 404.

### Token refresh / keep-alive (server/index.ts ~647)
- Background interval pings `listOneDriveItems('/')` every 25 min (refresh well before MS 60-min expiry).
- Updates `(globalThis as any).__odLastVerify = { ts, result }`.
- Status pill in dashboard.tsx (~39498) reads from `/api/onedrive/status`. Green ●=healthy, expanded red=broken (opens in-app reauth modal, no new tab).
- Personal MS accounts rotate refresh tokens. If Pi offline >24h, token DIES — only device-code flow recovers it.

### File text extraction & Library search
- `files` table columns: id, folder, original_name, display_name, object_path, content_type, size, extracted_text, prepared_audio_paths, total_chunks, listened, last_chunk_index, checked_chunks, tts_audio_url, tts_generated_at, prepared_at, created_at. **NO `name` column** — use `original_name` or `display_name`.
- `extracted_text` is NULL until first read. Trigger lazy extraction by `GET /api/files/:id/text` — extracts, caches in DB, returns `{text}`.
- Library search (~routes.ts:5605) only finds files where `extracted_text IS NOT NULL`. If Bryn says "search isn't finding X", first check `SELECT id, folder, LENGTH(extracted_text) FROM files WHERE folder = '<week>-<course>-module'` and trigger extraction for any with NULL.
- Folder naming convention: `week-N-<courseCodeLower>-<type>` where type is `module` or `reading`. Examples: `week-1-cfnf400-module`, `week-11-cppa122-reading`.

### Semester pipeline health (server/routes.ts:4026)
- `oneDriveFolderConfigured` is TRUE if EITHER (a) legacy text fields populated OR (b) the live OneDrive token works (fallback uses cached `__odLastVerify`).
- Pipeline `n/5 healthy` count must match icon dot count. Storage always green (Replit object storage is always reachable). OneDrive/Sync/Library go green together when folder mapped.
- Naming-convention rows in flow modal use the same booleans as the pipeline icons — never compute differently or they'll disagree visually.

### HW/Notes coupling (dashboard.tsx)
- `homeworkMinimized` / `blankBoxOpen` / `blankBoxMinimizedToTab` localStorage keys MUST be cleared on mount. HW box defaults open every page load.
- 4 swap handlers + `hwShrinkToTab` / `hwGrowFromTab` keyframes in index.css. Add a 5th entry point only if you wire all the same coordination.

### BA wizard styling guards (server/aiCommandTools.ts update_app_theme)
- Server rejects same-luminance bubble-bg ↔ text-color combos. Pair them in one call.
- `wizardUndo: true` reverts to last snapshot. `wizardReset: true` factory defaults.
- Frontend ALSO ignores wizardTextColor when luminance ≈ bubble bg (double safety).

### Auth header on internal calls
- Tools that hit `/api/...` from BA must include `x-auth-level: 5747` or get 403. The internal http_check tool already does this.

### Big-file rules (still apply)
- `dashboard.tsx` > 39000 lines, `routes.ts` > 28000 lines, `aiCommandTools.ts` > 6000 lines. ALWAYS read with offset/limit. `tsc --noEmit` hangs on dashboard.tsx — rely on Vite HMR build success in workflow logs instead.

### Known noise to ignore
- `duplicate key value violates unique constraint "shift_schedule_date_unique"` from AutoImport bulk insert at startup — non-fatal, pre-existing, NOT a bug to chase.

### Git push template (memorize)
```
git add -A && git commit -m "<msg>" && \
  git push "https://x-access-token:${GITHUB_PERSONAL_ACCESS_TOKEN7}@github.com/Reachushere/Home-View.git" main && \
  git rev-parse HEAD
```
### 2026-04-17 — BA self-sufficiency tools live
- What: memory_append, pi_deploy, onedrive_reauth_start are wired in routes.ts (readOnly + coreTool sets) and aiCommandTools.ts. System prompt §3.6 teaches usage.
- Why: Verifying the new tools work end-to-end.
### 2026-04-18 — PDF page citations + Library carousel overlap
- **Page citations now work in Study Assistant**: PDF extraction in `/api/files/:id/text` (server/routes.ts ~8580) joins pages with sentinel ` XPGBRKX `, runs cleanTextForTTS, then converts surviving sentinels to numbered `[Page N]` markers. cleanTextForTTS still strips `---PAGE---` (line 503), so DON'T revert to that marker.
- **`?force=1`** query param on `/api/files/:id/text` re-extracts. Endpoint also auto-re-extracts if cached `extracted_text` lacks `[Page N]` markers.
- **chat-materials prompt** (server/routes.ts ~6091) now mandates `(Course, Module, p. N)` citations using the `[Page N]` markers in context. AI must NOT invent page numbers.
- To re-extract a course's modules in bulk: `for id in 148 151 152 ...; do curl -s "http://localhost:5000/api/files/$id/text?force=1"; done`
- **Library overlap fix**: Notepad button (last in semester carousel) was at right:164px, overlapping APA Checker at right:156px. Carousel moved to right:200px. Top-right z-stack is: carousel (right:200) | APA (right:156) | Share (right:72) | Close (right:28). Don't shrink the carousel right value below 200px or notepad will collide with APA again.

═══════════════════════════════════════════════════
KNOWLEDGE TRANSFER — Apr 19-20 2026 (Grades + GPA + Course Detail)
═══════════════════════════════════════════════════

### 2026-04-20 — `tasks.is_not_graded` column (NEW)
- Where: shared/schema.ts (`isNotGraded: boolean("is_not_graded").default(false)`) + migration ran via direct `psql "$DATABASE_URL" -c "ALTER TABLE tasks ADD COLUMN is_not_graded BOOLEAN DEFAULT FALSE NOT NULL"`
- What: Replaces the old "type-based" inference. `isTaskUngraded(task) = task.isNotGraded === true || task.type === 'discussion'`. Discussions stay auto-ungraded for legacy reasons.
- Why: Bryn wanted explicit control via a "Graded vs Not Graded" toggle in the task form. Don't infer ungraded from any other type.

### 2026-04-20 — drizzle-kit push hangs on `degree_tracking_data_key_unique`
- Where: `npm run db:push` interactive prompt
- What: drizzle-kit asks about renaming `degree_tracking_data_key_unique` constraint and hangs forever waiting on stdin (no good way to answer in non-interactive shell).
- Why it matters: For new column adds, BYPASS drizzle-kit. Use direct SQL: `psql "$DATABASE_URL" -c "ALTER TABLE <table> ADD COLUMN ..."`. Drizzle's runtime accepts the column once it exists in the DB and the schema.ts type matches.

### 2026-04-20 — `excludeFromGpa` greys row in place (CourseDetailDialog)
- Where: client/src/components/CourseDetailDialog.tsx renderAssignmentRow (~line 1700) — graded row applies opacity:0.35 when `task.excludeFromGpa === true`
- What: Toggling "Grade Received" off (the green→grey switch) keeps the assignment in the Graded section but dims it AND removes it from numerator/denominator. Previously it moved to Ungraded — DO NOT restore that behavior.
- Why: Bryn wants visual continuity (assignments stay where she put them).

### 2026-04-20 — Section rename: "Ungraded" → "Not Graded"
- Where: CourseDetailDialog.tsx section header text
- What: User-facing label is "Assignments (Not Graded)". Internal variable names (`ungradedCourseTasks`, `sortedUngradedTasks`, `renderUngradedAssignmentRow`) kept for code stability.
- Why: Grammatical accuracy — "Not Graded" reads better in the form toggle.

### 2026-04-20 — Active grade sets (memorize these — used everywhere)
- Where: CourseDetailDialog.tsx near top of component
- What:
  - `gradedCourseTasks` = raw graded tasks (includes scratched + excluded)
  - `includedGradeTasks` = `gradedCourseTasks.filter(t => !t.excludeFromGpa)` — drives `totalWeight`
  - `activeGradeTasks` = `includedGradeTasks.filter(t => !t.gradeScratchedOff)` — drives the bottom Totals row score/total/weight sums AND the percent calc
  - `scratchedWeight` / `activeWeight` = totalWeight − scratchedWeight
- Why: Three different "is this task counted?" questions. Use the right one or numbers will be wrong (e.g., Total column was summing `gradedCourseTasks` and inflating to 111 because the scratched task's max-marks of 80 was being counted).

### 2026-04-20 — Inline semester GPA badge in course header
- Where: CourseDetailDialog.tsx course header (left of "COMPLETE" pill)
- What: Bordered blue pill `border-blue-400/40 bg-blue-400/10`, shows `GPA 3.78 A- · 84.0%`. Uses letter→% approximation when an explicit % isn't available. Returns null when no graded courses in the semester.
- Why: Fast at-a-glance read without opening the GPA box.

### 2026-04-20 — Sortable Done / Scratch / Grade Received columns
- Where: CourseDetailDialog.tsx — `SortField` extended with `'done' | 'scratched' | 'gradeReceived'`; cmp switch handles them as boolean (false<true)
- What: Click the column header to sort. Booleans go false-first in asc (so `desc` surfaces the active rows on top). Indicator arrow appears next to the active label.
- Why: Bryn asked for it; pattern matches existing Score/Total/Weight/Percent sorts.

### 2026-04-20 — Strikethrough policy
- Where: CourseDetailDialog.tsx lines ~1722 (graded) and ~2004 (ungraded)
- What: Completed assignments — NO strikethrough. Just dim to `text-white/50`. Green check carries the "done" signal.
  Scratched-off assignments (graded only) — KEEP strikethrough + `text-red-400/60`. Different state, deserves the cross-out.
- Why: Bryn finds completed-with-strikethrough hard to scan; scratched-with-strikethrough is desirable.

### 2026-04-20 — GPA box "between semesters" fallback
- Where: GPA box logic in dashboard.tsx — uses `getCurrentSemKey()` from shared/semesterUtils.ts
- What: When `getCurrentSemKey()` returns `''` (gap weeks between semesters), walks `SEMESTER_DATE_RANGES` to find most recently ENDED semester. UI then collapses to a single "TO LAST SEMESTER" pill (includes all completed including the just-ended one); hides the "CURRENT" pill + divider entirely. `inActiveSemester` flag is derived from the RAW `getCurrentSemKey()` (not the fallback).
- Why: Box was blank between semesters. Don't reintroduce a separate CURRENT pill in the gap — the consolidated TO LAST SEMESTER reads cleaner.

### 2026-04-20 — Past-semester grade editing (NO readOnly guard)
- Where: CourseDetailDialog.tsx
- What: There is no `readOnly`/locked-by-semester check. Edits to past-semester course grades propagate via `onGradeCalculated` → `courseGrades` → GPA box live. This is INTENTIONAL — Bryn often back-fills old course grades.
- Why: Don't add a "this semester is over, grades locked" guard unless explicitly asked.

### 2026-04-20 — Course Detail column alignment (fragile)
- Where: CourseDetailDialog.tsx header ~line 4465 + data row inline-grade cells + totals row ~4548
- What: All three rows share `gap: 10px` and `position: relative; left: -12px` on the inline-grade cluster. Header label `left` offsets are: Score -6, Total -6, Weight -2, Percent -2. Hidden-spacer in totals row mirrors the right-side Scratch (18×14, ml -10, mr 2) / GradeReceived (24×14, ml 8) / Copy (15×15, ml 14) / Delete (15×15) widths.
- Why: If you change a column width or gap on one row, change it on ALL THREE or the columns will visually drift. Visibility-hidden spacers in the totals row exist purely to push the trailing icons off-screen while preserving column alignment with rows above.

### 2026-04-20 — Pending UI debt (so BA can volunteer fixes)
- Snowflake `snowSpin` @keyframes still missing in client/src/index.css (snowfall component refs it).
- Sunday left-column borderLeft removal pending in 4 sites (semester grid weekend dividers).
- Hardcoded `semKeyOrder` arrays in dashboard.tsx + monthlyReportEmail.ts don't include w2030+ — extend ranges proactively when working in those areas.
- 500 errors on initial dashboard load (intermittent), 2AM timeline rendering glitch, Calendar Phase 2 refactor, BA chat yellow-tint styling — all open.
- BLOCKED (don't touch without Bryn's go-ahead): UniCal logo "switch", profile settings scroll, profile photo double-click handler.

### 2026-04-20 — File size watermarks (update prior estimates)
- dashboard.tsx is now ~41,200+ lines (was 39000). Read tool caps; for content beyond ~line 19,274 use `sed -n 'START,ENDp'` via bash.
- routes.ts ~28,000 lines (stable).
- CourseDetailDialog.tsx ~4,600 lines.

### 2026-04-20 — Default values for new task creation
- New tasks default to: `excludeFromGpa: true`, `isNotGraded: false`. Result: Graded section shows it greyed-out until score/total/weight are filled in. Once Bryn enters the grade, she flips Grade Received toggle on (excludeFromGpa: false) to include it.
- Why: Prevents incomplete assignments from polluting the running GPA.

## Apr 20 — Auto-recreate fix, label shift, drag-drop sections, ungraded header
- **"read automation report" auto-recreate bug**: `pi-import-tasks.json` had id 1098 with that title. AutoImport on every server start re-seeded it (dedup key = stripped-title+easternDate, which doesn't know about user deletions). Removed entry from JSON (139→138) and DELETE FROM tasks WHERE id=1098.
- **Column label shift (CourseDetailDialog.tsx ~line 4466)**: header label `left:` offsets bumped 3px more left → Score -9, Total -9 (editing -10), Weight -5 (editing -6), Percent -5. Did NOT touch Scratch/GradeReceived/Copy. Data row + totals row both at `left:-12px` so they already column-align.
- **Not Graded section header (~line 4577)**: removed standalone "GPA" pill. Replaced with right cluster mirroring row: `gap:6px, marginRight:15px`, "X items" in 24px-wide left-aligned slot + 15px trash spacer. Now "items" label sits over the toggle column.
- **Drag-drop between sections**: added `dragSourceSection` + `dragOverSection` state. `handleDragStart(taskId, source)` now takes source; ungraded rows pass 'ungraded'. New `handleSectionDragOver`/`handleSectionDrop` on both list containers (`assignments-list`, `ungraded-assignments-list`). Cross-section drop calls `updateTaskMutation`: → graded sets `{isNotGraded:false, excludeFromGpa:false}`; → ungraded sets `{isNotGraded:true, gradeScratchedOff:false}`. Hover shows blue outline. Same-section drops fall through to existing reorder logic.
